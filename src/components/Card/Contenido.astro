---
interface Props {
  title?: string;
  body?: string;
}

const { title, body } = Astro.props;
---
<>
  {title && <h1 class="text-xl font-bold m-0 z-20">{title}</h1>}
  {body && <p class="m-0 font-light text-base">{body}</p>}
  <slot />
</>